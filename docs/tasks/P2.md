# P2 — SEO

> เฟสก่อนหน้า: P1 · ขนานกับ P3, P4 · ขนาด M · 10 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p2`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §6
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P2-01…P2-09 · 🟥 BE/ops → P2-10

**เป้าหมาย:** Google เก็บหน้าได้ครบ · แชร์ Facebook แล้วขึ้นการ์ดถูกทรัพย์

**ขอบเขต:** metadata + structured data + landing page · ไม่แตะ business logic

---

### P2-01 — generateMetadata หน้าทรัพย์
- [ ] title/description จากข้อมูลจริง (เช่น "ที่ดิน 3-2-20 ไร่ โซนเชียงคาน 9.6 ล้าน | Loeiland")
- [ ] `alternates.canonical` + `alternates.languages` (th/en)
- [ ] `openGraph` + `twitter` fields
- [ ] commit `P2-01: ...`

### P2-02 — JSON-LD หน้าทรัพย์
- [ ] `RealEstateListing` + nested `Place` (โซน ไม่ใช่พิกัดจริง) + `Offer` (price, priceCurrency THB)
- [ ] render เป็น `<script type="application/ld+json">` (ไม่ใช่ `dangerouslySetInnerHTML` บน element ทั่วไป — ใช้ pattern ปลอดภัย)
- [ ] commit

### P2-03 — JSON-LD หน้าแรก
- [ ] `RealEstateAgent` / `LocalBusiness` — ชื่อ, พื้นที่บริการ = จังหวัดเลย, ช่องทางติดต่อ
- [ ] commit

### P2-04 — sitemap.ts
- [ ] `src/app/sitemap.ts` — ทรัพย์ approved ทุกตัว + landing (อำเภอ 14 + ประเภท) + หน้า static, ทั้ง 2 locale
- [ ] `lastModified` จาก `updated_at`
- [ ] commit

### P2-05 — robots.ts
- [ ] allow ทั้งหมด, ชี้ sitemap, `disallow: /admin`
- [ ] commit

### P2-06 — landing รายอำเภอ × 14
- [ ] `/[locale]/district/[district]/page.tsx` (ISR) + `generateStaticParams` จาก `districts`
- [ ] intro ต่ออำเภอ (เขียนหรือ template) + รายการทรัพย์ในอำเภอ + จำนวน
- [ ] JSON-LD `CollectionPage` + `generateMetadata`
- [ ] ลิงก์ภายในจากหน้าแรก / หน้าทรัพย์ (breadcrumb)
- [ ] commit

### P2-07 — landing รายประเภท
- [ ] `/[locale]/type/[type]/page.tsx` (ISR) — โครงเดียวกับอำเภอ
- [ ] commit

### P2-08 — OG image อัตโนมัติ
- [ ] `opengraph-image.tsx` หน้าทรัพย์ — รูป cover + ราคา + ขนาด + โซน + โลโก้ (`next/og`)
- [ ] fallback OG image หน้า static
- [ ] commit

### P2-09 — next/image + alt + metadataBase
- [ ] แปลง `<img>` ทั้งหมดเป็น `next/image` + `sizes`
- [ ] alt ภาษาไทยจาก `property_photos.alt_th` (fallback title)
- [ ] ตั้ง `metadataBase` = `NEXT_PUBLIC_SITE_URL` ใน root layout
- [ ] commit

### P2-10 — ตรวจ + ส่ง (ops)
- [ ] Rich Results Test ผ่าน: หน้าทรัพย์ 1 ตัวอย่าง + landing 1 อำเภอ
- [ ] Lighthouse SEO = 100 บนหน้าแรก + หน้าทรัพย์
- [ ] ส่ง sitemap เข้า Google Search Console + verify domain
- [ ] สร้าง Google Business Profile (**ops** — นอกโค้ด)

---

## เกณฑ์ว่าเฟส P2 เสร็จ

- Rich Results Test ไม่มี error บนหน้าทรัพย์ + landing 1 อำเภอ
- `curl` หน้าทรัพย์เห็น `og:*` + JSON-LD ครบ
- Lighthouse SEO = 100

## กติกาปิด task

1. ติ๊ก subtask · ครบแล้วติ๊ก `### P2-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 10` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P2-XX: <สิ่งที่ทำ>`
