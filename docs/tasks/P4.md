# P4 — ฟอร์มสาธารณะ

> เฟสก่อนหน้า: P1 · ขนานกับ P2, P3 · ปลดล็อก: P5 · ขนาด M · 10 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p4`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §5, §9
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P4-02, P4-09 · 🟥 BE → P4-04, P4-05, P4-06, P4-07, P4-08, P4-10 · 🟨 ร่วม → P4-01, P4-03

**เป้าหมาย:** lead / ฝากขาย / ต้องการซื้อ เข้า DB ถูกตาราง ผ่านการกันบอท + rate limit · แอดมินได้แจ้งเตือน

**ขอบเขต:** เฉพาะ write path สาธารณะ · หน้าแอดมินตรวจ = P5 · form fields อ้างจาก `mock-up.html` (leadForm `:1121`, submitPropertyForm `:1146`, buyerRequestForm `:1257`)

---

### P4-01 — Zod schemas
- [ ] `src/lib/schemas/lead.ts` `submitProperty.ts` `buyerRequest.ts` — field + ข้อความ error 2 ภาษา
- [ ] `submitProperty`: แยกชัดว่าฟิลด์ไหนสาธารณะ / ฟิลด์ไหนเข้า `property_private` (owner, พิกัด, note, consent)
- [ ] `cleanText` equivalent (trim, ตัด `<>`) — เทียบ `mock-up.html:1817`
- [ ] commit `P4-01: ...`

### P4-02 — React Hook Form
- [ ] ต่อ `zodResolver` ทั้ง 3 ฟอร์ม
- [ ] ฟอร์มฝากขาย (~20 ช่อง) แบ่ง 3 สเต็ป + progress + validate ต่อสเต็ป (`PLAN.md` §7)
- [ ] `inputmode="numeric"` ช่องราคา/ไร่/งาน/วา
- [ ] commit

### P4-03 — Turnstile
- [ ] `<Turnstile>` widget ทั้ง 3 ฟอร์ม + `NEXT_PUBLIC_TURNSTILE_SITE_KEY`
- [ ] block submit จน token พร้อม
- [ ] commit

### P4-04 — rate limit
- [ ] `src/lib/ratelimit.ts` — `@upstash/ratelimit` sliding window per IP (**D-6**)
- [ ] `src/lib/ip.ts` — `hashIp()` (เก็บ hash เท่านั้น ไม่เก็บ IP ดิบ)
- [ ] env `UPSTASH_REDIS_REST_URL` / `_TOKEN`
- [ ] commit

### P4-05 — Server Action: submitLead
- [ ] `src/app/actions/submitLead.ts` — `'use server'`
- [ ] ลำดับ: parse Zod → verify Turnstile กับ Cloudflare → ratelimit → insert `leads` (service role) + `property_id` + `consent_at` + `consent_version` + `ip_hash`
- [ ] คืน result type ให้ client แสดง toast (เทียบ `i18n.contactSaved`)
- [ ] commit

### P4-06 — Server Action: submitProperty
- [ ] parse → Turnstile → ratelimit
- [ ] insert `properties` (state='pending', เฉพาะฟิลด์สาธารณะ) **+** `property_private` (owner_name, owner_contact, exact_lat/lon ที่กรอก, deed_number, admin_note, consent) — ธุรกรรมเดียว (rpc/transaction)
- [ ] **ห้าม** ใส่ owner/พิกัดจริงลง `properties`
- [ ] commit

### P4-07 — Server Action: submitBuyerRequest
- [ ] parse → Turnstile → ratelimit → insert `buyer_requests` (+ `target_property_id` ถ้ามี)
- [ ] commit

### P4-08 — แจ้งเตือนแอดมิน
- [ ] `src/lib/notify.ts` — `sendAdminEmail` (Resend) + `pushLine` (LINE Messaging API, `LINE_ADMIN_USER_ID`)
- [ ] เรียกหลัง insert สำเร็จ · ห่อ try/catch — notify ล้มไม่ทำให้ response ผู้ใช้ล้ม
- [ ] เนื้อหา: ประเภทฟอร์ม + code/ชื่อ + ลิงก์ไป `/admin`
- [ ] _(ต้องมี LINE OA จริง — **D-3**)_
- [ ] commit

### P4-09 — UX สำเร็จ / ผิดพลาด
- [ ] toast + reset form + ข้อความ 2 ภาษา (เทียบ `showToast` + `i18n.reviewSaved`/`buyerSaved`)
- [ ] แสดง error รวม (Turnstile fail / rate limited / server error) แบบ inline
- [ ] disable ปุ่มระหว่างส่ง
- [ ] commit

### P4-10 — เทสต์ RLS / gateway
- [ ] `tests/contract/anon-write.test.ts` — anon key `insert` เข้า `leads` / `buyer_requests` / `properties` ตรง ๆ → **ถูกปฏิเสธ**
- [ ] ผ่าน Server Action → สำเร็จ
- [ ] เรียก action ซ้ำเร็ว ๆ → โดน rate limit (429/ผลลัพธ์ limited)
- [ ] commit

---

## เกณฑ์ว่าเฟส P4 เสร็จ

- ส่งครบ 3 ฟอร์ม → แถวเข้า DB ถูกตาราง · owner PII อยู่ใน `property_private` เท่านั้น · ได้อีเมล + LINE
- ยิง action ซ้ำเร็ว ๆ โดน rate limit
- anon insert ตรง ๆ ถูก RLS ปฏิเสธ (เทสต์ `P4-10`)

## กติกาปิด task

1. ติ๊ก subtask · ครบแล้วติ๊ก `### P4-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 10` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P4-XX: <สิ่งที่ทำ>`
