# P6 — เตรียมเปิดจริง

> เฟสก่อนหน้า: P5 (และ P2, P3 เสร็จ) · ขนาด M · 10 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p6`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §5
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P6-01, P6-07, P6-08 · 🟥 BE → P6-03, P6-04, P6-06, P6-09, P6-10 · 🟨 ร่วม → P6-02, P6-05

**เป้าหมาย:** ผ่าน checklist ความปลอดภัย/PDPA ครบ · เว็บไม่ดูร้าง · วัดผลได้

---

### P6-01 — /privacy + /terms ฉบับเต็ม
- [ ] `/[locale]/privacy` — ผู้ควบคุมข้อมูล, ฐานทางกฎหมาย, ประเภทข้อมูลที่เก็บ, ระยะเวลาเก็บ, สิทธิเจ้าของข้อมูล (ม.30–33), ช่องทางร้องเรียน (ขยายจาก `privacyDrawer` `mock-up.html:1355`)
- [ ] `/[locale]/terms` — ข้อจำกัดความรับผิด, ข้อมูลเป็นเบื้องต้นไม่ใช่คำเสนอขาย (เทียบ `privacyTerm1–3`)
- [ ] เวอร์ชัน consent = อ้างอิงหน้านี้ (`consent_version`)
- [ ] commit `P6-01: ...`

### P6-02 — data subject request
- [ ] `/[locale]/data-request` — ฟอร์มขอดู/แก้/ลบข้อมูล (ผ่าน Server Action + Turnstile)
- [ ] `/admin/data-requests` — คิว + ปุ่ม "ดำเนินการ" (ค้นหา lead/buyer/property ตามเบอร์ → export หรือ hard delete)
- [ ] ทุกการดำเนินการ log `audit_log`
- [ ] commit

### P6-03 — retention cron
- [ ] Supabase scheduled function (pg_cron) — ลบ `leads`/`buyer_requests` ที่ `crm_status ∈ {ปิดดีล, ยกเลิก}` และเก่ากว่า 18 เดือน
- [ ] เขียนสรุปจำนวนที่ลบลง `audit_log`
- [ ] ทดสอบด้วยข้อมูล fixture
- [ ] commit

### P6-04 — Sentry เต็ม
- [ ] source maps upload ตอน build + release tag = git sha
- [ ] alert rule → อีเมล
- [ ] ตรวจ PII scrubbing (ไม่ส่งเบอร์/ชื่อเข้า Sentry)
- [ ] commit

### P6-05 — analytics
- [ ] ตัดสิน **D-7** (Vercel Analytics vs Plausible)
- [ ] ติดตั้ง + ยืนยันไม่ต้องขอ cookie consent (PDPA-friendly)
- [ ] event: เปิดหน้าทรัพย์, กด CTA โทร/LINE, ส่งฟอร์ม
- [ ] commit

### P6-06 — Playwright e2e
- [ ] setup `@playwright/test` + CI job
- [ ] เคส A: anon เรียก REST `property_private` / เบอร์ lead → 401 / empty
- [ ] เคส B: view-source หน้าทรัพย์ (fixture owner=`SECRET`) → ไม่มีสตริง `SECRET`
- [ ] เคส C: flow submit → approve (login แอดมิน) → ทรัพย์ปรากฏหน้าเว็บ + พิกัดต่างจากที่กรอก
- [ ] commit

### P6-07 — perf budget
- [ ] Lighthouse CI mobile ≥ 90 ทุกหมวด: หน้าแรก + หน้าทรัพย์ + landing
- [ ] ยืนยัน Longdo SDK ไม่อยู่ใน initial bundle หน้าแรก
- [ ] ตรวจ `next build` bundle report — ไม่มี `supabase/admin` / service key ใน client chunk
- [ ] commit

### P6-08 — เชื่อม FB / LINE จริง
- [ ] ตัดสิน **D-3** — มีเพจ Facebook + LINE OA จริง
- [ ] ใส่ `NEXT_PUBLIC_FACEBOOK_URL` + `NEXT_PUBLIC_LINE_ADD_FRIEND_URL`
- [ ] ปุ่ม CTA โทร/เพิ่มเพื่อน LINE ติดขอบล่างหน้าทรัพย์ (มือถือ)
- [ ] แทนที่ `APP_CONFIG.facebookUrl` placeholder ทุกจุด
- [ ] commit

### P6-09 — เนื้อหาเปิดตัว
- [ ] ตัดสิน **D-5** — ใครหาทรัพย์ + ถ่าย/ขอรูป
- [ ] seed 30–50 แปลงจริง (ผ่าน flow แอดมินปกติ ไม่ใช่ SQL ตรง) + รูปจริง ≥ 3/แปลง
- [ ] ตรวจ landing รายอำเภอทุกหน้ามีทรัพย์อย่างน้อย 1 (ไม่ว่าง)
- [ ] commit

### P6-10 — ปิด decision + go-live
- [ ] **D-1** Longdo commercial licensing — ได้คำตอบเป็นลายลักษณ์อักษร
- [ ] **D-2** Vercel Pro — บัญชี + การจ่ายเงินพร้อม, ย้าย deployment เป็น Pro
- [ ] เชื่อมโดเมนจริง + SSL + `NEXT_PUBLIC_SITE_URL`
- [ ] 404/500 page, canonical, `noindex` หลุดออกหมด
- [ ] ทุกแถว decision ใน `PROGRESS.md` = ✅

---

## เกณฑ์ว่าเฟส P6 เสร็จ

- Playwright e2e เขียวทั้ง 3 เคส (A/B/C)
- Lighthouse mobile ≥ 90 ทุกหมวด
- ทุกแถว decision (D-1…D-7) ใน `PROGRESS.md` = ✅

## กติกาปิด task

1. ติ๊ก subtask · ครบแล้วติ๊ก `### P6-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 10` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P6-XX: <สิ่งที่ทำ>`
