# P3 — แผนที่ Longdo

> เฟสก่อนหน้า: P1 · ขนานกับ P2, P4 · ขนาด M · 8 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p3`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §2 (ข้อ 6), §7
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟥 BE/ops → P3-01 · 🟦 FE → P3-02…P3-08

**เป้าหมาย:** แผนที่โซนทำงานเทียบเท่า prototype แต่ลื่นบนมือถือ · ไม่ดึง OSM tile เอง

**ขอบเขต:** แทนที่ map ที่เขียนมือ (`project`/`unproject`/`tileUrl`/`renderMap` `mock-up.html:1857–1986`) ด้วย Longdo SDK · ไม่แตะ business logic

---

### P3-01 — Longdo API key
- [ ] ขอ key ที่ [api.longdo.com/console](https://api.longdo.com/console/)
- [ ] จำกัด HTTP referrer (localhost + โดเมน production/preview)
- [ ] ใส่ `NEXT_PUBLIC_LONGDO_MAP_KEY` (local + Vercel)
- [ ] _(D-1 เงื่อนไขเชิงพาณิชย์ยังค้าง — dev ใช้ไปก่อน)_

### P3-02 — `<ZoneMap>` client component + lazy load
- [ ] `components/map/ZoneMap.tsx` — `'use client'`
- [ ] โหลด Longdo SDK script แบบ lazy: เมื่อ component เข้า viewport (`IntersectionObserver`) หรือกดปุ่ม "ดูแผนที่"
- [ ] init map ที่ center จังหวัดเลย (`17.486, 101.58`), zoom ~10
- [ ] cleanup ตอน unmount
- [ ] commit `P3-01/02: ...`

### P3-03 — หมุดจากทรัพย์ที่ filter อยู่
- [ ] รับ props `properties: PublicProperty[]` + `activeId`
- [ ] วาง marker หมายเลข + ป้าย `zone` + ราคา (เทียบ marker `mock-up.html:1960–1985`)
- [ ] อัปเดต marker เมื่อ list เปลี่ยน (ไม่ทำลาย map instance)
- [ ] commit

### P3-04 — วงกลมโซน
- [ ] วาด circle รัศมี `zone_radius_m` รอบแต่ละหมุด (Longdo overlay) — สื่อ "บริเวณ" ไม่ใช่จุด
- [ ] สไตล์ตาม `--brand` (เทียบ `.zone-ring`)
- [ ] commit

### P3-05 — POI dots
- [ ] รับ `poi` จาก `getPoi()` → วาง marker รอง (ไอคอน/ตัวอักษร) เทียบ `mock-up.html:1950–1958`
- [ ] commit

### P3-06 — preview popup
- [ ] hover (desktop) / tap (mobile) หมุด → popup: รูป + ราคา + `code` + `zone` + ขนาด + จุดเด่นแรก (เทียบ `showMapPreview` `mock-up.html:1893`)
- [ ] จัดตำแหน่ง popup ไม่ให้ล้นขอบแผนที่
- [ ] commit

### P3-07 — sync 2 ทาง กับ list + URL
- [ ] คลิกหมุด → nav ไป `/property/[slug]` (หรือ highlight การ์ด)
- [ ] hover/เลือกการ์ด → แผนที่ `pan` ไปหา + เปิด popup
- [ ] แผนที่ใช้ filter ชุดเดียวกับ list (อ่านจาก URL params เดียวกัน P1-10)
- [ ] commit

### P3-08 — มือถือ + fallback
- [ ] ปุ่มลอยสลับ list ⇄ map เต็มจอ (`PLAN.md` §7)
- [ ] ถ้า SDK โหลดไม่ขึ้น → กล่องข้อความ + ลิงก์ดูรายการแทน (ไม่ให้หน้าเจ๊ง)
- [ ] ตรวจ Network tab: ไม่มี request `tile.openstreetmap.org` · SDK โหลดเฉพาะเมื่อเปิดแผนที่
- [ ] commit

---

## เกณฑ์ว่าเฟส P3 เสร็จ

- filter แล้วหมุดบนแผนที่เปลี่ยนตาม
- ไม่มี request ไป `tile.openstreetmap.org`
- Longdo SDK ไม่โหลดจนกว่าจะเปิดแผนที่ (ยืนยันใน Network tab)

## กติกาปิด task

1. ติ๊ก subtask · ครบแล้วติ๊ก `### P3-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 8` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P3-XX: <สิ่งที่ทำ>`
