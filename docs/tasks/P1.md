# P1 — ข้อมูล + หน้าอ่านอย่างเดียว

> เฟสก่อนหน้า: P0 · ปลดล็อก: P2, P3, P4 · ขนาด L · 15 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p1`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §4–§7
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P1-07…P1-14 · 🟥 BE → P1-01, P1-02, P1-03, P1-04, P1-06, P1-15 · 🟨 ร่วม → P1-05

**เป้าหมาย:** เปิดเว็บเห็นทรัพย์จริงจาก DB กรองครบทุกตัวกรองของ prototype — ยังไม่มีแผนที่ ไม่มีฟอร์ม

**ขอบเขต:** อ่านอย่างเดียว (anon) · ไม่มี auth · ไม่มี write · แผนที่/ฟอร์ม/แอดมิน = เฟสอื่น

---

### P1-01 — migration: properties + private + photos
- [ ] `supabase/migrations/0001_core_properties.sql` — `properties` ตาม `PLAN.md` §4.1 + `total_wah` generated column
- [ ] index: `(state)`, partial `(district) where state='approved'`, partial `(total_wah) where state='approved'`
- [ ] `property_private` + FK `on delete cascade`
- [ ] `property_photos`
- [ ] trigger `updated_at` (`moddatetime`)
- [ ] `supabase db push` + ตรวจใน dashboard
- [ ] commit `P1-01: ...`

### P1-02 — migration: leads / CRM / geo
- [ ] `0002_leads_crm.sql` — `leads` `buyer_requests` `crm_events` `audit_log`
- [ ] `0003_geo.sql` — `districts` `subdistricts` `poi`
- [ ] check constraint: `state` (5 ค่า), `crm_status` (6 ค่าไทยจาก `mock-up.html:1552`)
- [ ] `db push` + commit

### P1-03 — RLS
- [ ] `0004_rls.sql` — `enable row level security` ทุกตาราง
- [ ] `properties`: anon `select` where `state='approved'` · authenticated ทั้งหมด
- [ ] `property_private` `leads` `buyer_requests` `crm_events` `audit_log`: ไม่มี policy anon (ปิดสนิท)
- [ ] `property_photos`: anon select เฉพาะรูปของทรัพย์ approved
- [ ] `districts` `subdistricts` `poi`: anon select
- [ ] ทดสอบด้วย anon key: `select` จาก `property_private` → 0 แถว / error
- [ ] commit

### P1-04 — data-access layer
- [ ] `src/lib/db/properties.ts` — `getApprovedProperties(filters)` `getPropertyBySlug` `getPropertiesByDistrict` `getPropertiesByType`
- [ ] type `PublicProperty` — ระบุฟิลด์ชัด **ไม่มี** `owner_*` `exact_*` `deed_number` `admin_note` `encumbrance`
- [ ] ทุกฟังก์ชัน public ใช้ anon client เท่านั้น (ห้าม import `supabase/admin`)
- [ ] `src/lib/db/geo.ts` — `getDistricts` `getPoi` `getSubdistricts(district)`
- [ ] commit

### P1-05 — utils หน่วยที่ดิน
- [ ] `src/lib/format.ts` — `totalWah` `ratePerRai` `ratePerWah` `formatSize(p, locale)` `formatMoney(v, locale)` (port `mock-up.html:1811–1855`)
- [ ] `src/lib/format.test.ts` — เคสจาก starterProperties (LL-0001 1-0-45 → 445 ตร.ว.; ราคา/ไร่, /ตร.ว.)
- [ ] `npm run test` เขียว
- [ ] commit

### P1-06 — seed
- [ ] หา dataset อำเภอ/ตำบลจังหวัดเลย (**D-4**) → `supabase/seed/districts.sql` `subdistricts.sql`
- [ ] `supabase/seed/properties.sql` — 5 แปลงจาก `starterProperties` (`mock-up.html:1392–1513`), `state='approved'`, `zone_lat/lon` = lat/lon เดิม, `zone_radius_m=800`, gen `slug` + `code` (`LL-0001`..`0005`)
- [ ] `property_private` แถวคู่ — owner สมมติ + `consent_at`/`consent_version`
- [ ] `poi.sql` — 6 แถวจาก `mock-up.html:1515–1522`
- [ ] `supabase db reset` รัน seed ผ่าน
- [ ] commit

### P1-07 — i18n dict
- [ ] แยก `i18n.th`/`i18n.en` (`mock-up.html:1556–1781`) เป็น namespace: `common` `filters` `property` `forms` `admin` `privacy`
- [ ] เขียนลง `messages/th.json` + `messages/en.json`
- [ ] แทนข้อความ hardcode ในหน้า placeholder ด้วย `useTranslations` / `getTranslations`
- [ ] commit

### P1-08 — หน้าแรก
- [ ] `/[locale]/page.tsx` (SSR, เรียก `getApprovedProperties`)
- [ ] `<SiteHeader>` — โลโก้, ปุ่มสลับภาษา (Link เปลี่ยน locale คง path), ปุ่มฝากขาย/ต้องการซื้อ/ติดต่อ
- [ ] `<Hero>` + `<Stats>` (จำนวนทรัพย์, "14 อำเภอ")
- [ ] `<PropertyList>` render การ์ด
- [ ] `<FilterBar>` UI เปล่า (logic ใน P1-10)
- [ ] commit

### P1-09 — การ์ดทรัพย์
- [ ] `<PropertyCard>` — รูป cover (`next/image` หรือ placeholder gradient), `code` badge, `listing_status`, title ตาม locale, meta (ประเภท/อำเภอ/ขนาด), ราคา + rate/ไร่ + /ตร.ว. (เทียบ `renderCards` `mock-up.html:2026`)
- [ ] ปุ่ม "ดูรายละเอียด" → `Link` `/property/[slug]`
- [ ] state active (hover/เลือก) เตรียม sync แผนที่ P3
- [ ] commit

### P1-10 — ตัวกรอง + /search
- [ ] `/[locale]/search/page.tsx` — อ่าน `searchParams`: `q type district subdistrict maxPrice minSize`
- [ ] params → filter ส่งเข้า `getApprovedProperties` (กรองฝั่ง server)
- [ ] keyword: `ilike`/`websearch` ใน id/title(th,en)/type/district/subdistrict/zone/tags/highlights (เทียบ `filterProperties` `mock-up.html:2007`)
- [ ] ตำบล dropdown ขึ้นกับอำเภอที่เลือก (เทียบ `populateFilters` `mock-up.html:1988`)
- [ ] bucket ราคา/ขนาด ตรง `mock-up.html:1046–1063`
- [ ] `<FilterBar>` push params ผ่าน `useRouter` + debounce keyword
- [ ] result count + ปุ่ม reset
- [ ] commit

### P1-11 — หน้าทรัพย์
- [ ] `/[locale]/property/[slug]/page.tsx` — `getPropertyBySlug`, `notFound()` ถ้าไม่เจอ/ไม่ approved
- [ ] `export const revalidate = 3600`
- [ ] แกลเลอรีรูป (`property_photos` เรียง `sort_order`, cover ก่อน)
- [ ] detail grid: ขนาด / ราคา / ราคา-ไร่ / ราคา-ตร.ว. / ประเภทเอกสาร / หน้ากว้าง / ถนน / สาธารณูปโภค — **ไม่มี** encumbrance / เลขโฉนด (ตัดจาก `mock-up.html:2109–2119`)
- [ ] จุดเด่น (list) + กล่องหมายเหตุข้อมูล (`legalNote`)
- [ ] POI ใกล้เคียง — haversine ฝั่ง server จาก `zone_lat/lon` เอา 3 ตัวใกล้สุด (port `distanceKm`/`nearbyList` `mock-up.html:2056–2071`)
- [ ] CTA: ปุ่ม "สนใจทรัพย์นี้" (hook ฟอร์ม P4) + ปุ่ม LINE/FB
- [ ] commit

### P1-12 — static params
- [ ] `generateStaticParams` — slug ทรัพย์ approved × 2 locale
- [ ] `dynamicParams = true` (ทรัพย์ใหม่ SSR ครั้งแรกแล้ว cache)
- [ ] commit

### P1-13 — empty / loading / error states
- [ ] `not-found.tsx` + `loading.tsx` (skeleton) ต่อ route group
- [ ] empty state ตัวกรอง (`i18n.empty`)
- [ ] `error.tsx` + Sentry capture
- [ ] commit

### P1-14 — mobile-first pass
- [ ] หน้าแรก/ค้นหา: การ์ด stack เต็มกว้าง
- [ ] `<FilterBar>` → bottom sheet (`<dialog>`) + ปุ่ม "ใช้ตัวกรอง" (ไม่กรองรัวทุกแตะ)
- [ ] target แตะ ≥ 44px · `inputmode="numeric"` ช่องตัวเลข
- [ ] ตรวจไม่มี `overflow:hidden` บน body · ทดสอบ viewport 360px
- [ ] commit

### P1-15 — contract test (public ไม่คืนฟิลด์ลับ)
- [ ] `tests/contract/public-fields.test.ts` — fixture ที่ `property_private.owner_name = 'SECRET'`
- [ ] เรียกทุก public query → assert ผลลัพธ์ไม่มีสตริง `SECRET` และไม่มี key `owner_` `exact_` `deed_number` `admin_note`
- [ ] เพิ่มเข้า CI
- [ ] commit

---

## เกณฑ์ว่าเฟส P1 เสร็จ

- กรองครบ 6 แบบ ให้ผลตรงกับ prototype
- `curl /th/property/<slug>` เห็น HTML เนื้อหาเต็ม (ไม่ต้องรัน JS)
- contract test `P1-15` เขียวใน CI

## กติกาปิด task

1. ติ๊ก subtask ระหว่างทำ · ครบแล้วติ๊ก `### P1-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 15` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P1-XX: <สิ่งที่ทำ>`
4. เจอเรื่องต้องตัดสินใจ → เพิ่มแถว "เรื่องที่ติดอยู่" ทันที
