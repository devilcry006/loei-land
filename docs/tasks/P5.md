# P5 — หลังบ้านแอดมิน

> เฟสก่อนหน้า: P4 · ขนาด L · 9 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p5`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §5, §10
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)**
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P5-03, P5-07 · 🟥 BE → P5-01, P5-02, P5-04, P5-05, P5-08, P5-09 · 🟨 ร่วม → P5-06

**เป้าหมาย:** แอดมินล็อกอินจริง ตรวจคิว อนุมัติแล้วทรัพย์ขึ้นเว็บ (พิกัดเบลออัตโนมัติ) มี CRM + audit

**ขอบเขต:** `/admin/*` เท่านั้น · แทนที่ `renderAdminQueue`/`approveSubmission`/`adminPin` ทั้งหมด (`mock-up.html:1336–2458`)

**ข้อควรระวังจาก prototype:** PIN ฝั่ง client (`:1389`), `innerHTML` ยัด input (`:2287`), merge owner fields เข้าทรัพย์สาธารณะตอน approve (`:2342`), เชื่อพิกัดที่เจ้าของกรอก

---

### P5-01 — Supabase Auth
- [ ] เปิด email+password, **ปิด public sign-up**, บังคับ MFA (TOTP)
- [ ] สร้างบัญชีแอดมินด้วยมือ (dashboard/SQL)
- [ ] `src/lib/auth.ts` — `getSessionUser()` server-side
- [ ] commit `P5-01: ...`

### P5-02 — middleware guard + ลบ PIN
- [ ] `middleware.ts` — `/admin/*` ไม่มี session → redirect `/admin/login`
- [ ] `/admin/login` — Supabase Auth UI (หรือ custom)
- [ ] `grep -r adminPin` / `grep -r "2468"` → ต้องไม่เหลือ
- [ ] `/admin` ไม่มี locale prefix (นอก `[locale]`)
- [ ] commit

### P5-03 — คิวตรวจ `/admin`
- [ ] 3 กลุ่ม: pending properties + buyer_requests + leads (เทียบ `renderAdminQueue` `:2276`)
- [ ] อ่านผ่าน server client (authenticated) — join `property_private` เพื่อโชว์ owner/พิกัดจริง **ให้แอดมินเท่านั้น**
- [ ] **ทุก field render เป็น text** (React escape) — ไม่มี `dangerouslySetInnerHTML`
- [ ] summary นับจำนวน (เทียบ `adminSummary`)
- [ ] commit

### P5-04 — approve
- [ ] Server Action `approveProperty(id)` — authenticated only
- [ ] เบลอ `exact_lat/lon` → สุ่ม offset 500–1,000 ม. → เขียน `properties.zone_lat/lon` + `zone_radius_m` (พิกัดจริงอยู่ `property_private` เท่านั้น)
- [ ] gen `code` (`LL-####` running) + `slug` (จาก title+อำเภอ+code)
- [ ] `state='approved'` + `published_at=now()`
- [ ] `revalidatePath` หน้าแรก + `/property/[slug]` + `/district/[district]` + sitemap
- [ ] commit

### P5-05 — reject + hard delete
- [ ] `rejectProperty(id)` → `state='rejected'` (เก็บไว้เพื่อ audit)
- [ ] ปุ่มแยก `deleteProperty(id)` → ลบ `properties` + `property_private` + รูปใน Storage → เขียน `audit_log`
- [ ] ยืนยัน 2 ชั้นก่อนลบ
- [ ] commit

### P5-06 — อัปโหลดรูป
- [ ] Supabase Storage **private bucket** `property-photos`
- [ ] **ล้าง EXIF/metadata ตำแหน่งฝั่ง server ก่อนเก็บ** — re-encode ด้วย `sharp` (strip metadata, จำกัดขนาด/คุณภาพ) · ไม่อ่าน/ไม่เก็บพิกัดจากรูป (กติกาเหล็ก `PLAN.md` §5 ข้อ 12)
- [ ] `/admin/properties/[id]` — อัปโหลดหลายรูป, ตั้ง `is_cover`, ลากจัด `sort_order`, กรอก `alt_th`/`alt_en`
- [ ] serve ผ่าน signed URL / custom `next/image` loader (public เห็นเฉพาะรูปของทรัพย์ approved)
- [ ] ทดสอบ: อัปรูปที่มี GPS EXIF → ไฟล์ที่เก็บใน Storage ไม่มี GPS tag
- [ ] commit

### P5-07 — CRM board
- [ ] `/admin/leads` + `/admin/buyer-requests` — เปลี่ยน `crm_status` (6 ค่า `mock-up.html:1552`)
- [ ] ทุกครั้งที่เปลี่ยน → insert `crm_events` (from/to/actor/note)
- [ ] filter ตามสถานะ + ลิงก์ไปทรัพย์ที่เกี่ยวข้อง (เทียบ `focusBuyerTarget` `:2444`)
- [ ] commit

### P5-08 — audit_log
- [ ] helper `logAudit(actor, action, entityType, entityId)`
- [ ] เรียกทุกครั้งที่หน้าแอดมินโหลด `property_private` หรือเบอร์ lead/buyer
- [ ] `/admin` มีหน้าอ่าน audit_log (เรียงใหม่สุด)
- [ ] commit

### P5-09 — export CSV
- [ ] Server Action / route — รวม leads + buyer_requests + pending + approved
- [ ] BOM `﻿` + escape `"` (เทียบ `exportCsv`/`csvEscape` `:2394–2442`)
- [ ] header ตาม `mock-up.html:2427`
- [ ] จำกัดเฉพาะ authenticated + log audit
- [ ] commit

---

## เกณฑ์ว่าเฟส P5 เสร็จ

- submit ทรัพย์ทดสอบ → เห็นในคิว → กดอนุมัติ → โผล่หน้าเว็บด้วยพิกัดที่ **ไม่ตรง** กับที่กรอก
- `property_private` ยังเก็บพิกัดจริง
- เปิดดูเบอร์เจ้าของ → มีแถวใหม่ใน `audit_log`
- `grep adminPin` ไม่เหลือ

## กติกาปิด task

1. ติ๊ก subtask · ครบแล้วติ๊ก `### P5-XX`
2. `../PROGRESS.md`: คืบหน้า `x / 9` + สถานะเฟส + บันทึกความคืบหน้า
3. commit `P5-XX: <สิ่งที่ทำ>`
