# P0 — ตั้งต้นโปรเจกต์

> เฟสก่อนหน้า: — · ปลดล็อก: ทุกเฟส · ขนาด S · 9 tasks
> ภาพรวม/เกณฑ์เฟส: [`../ROADMAP.md#p0`](../ROADMAP.md) · เหตุผล: [`../PLAN.md`](../PLAN.md) §3, §11
> **ปิด task แล้วอัปเดต [`../PROGRESS.md`](../PROGRESS.md)** (กติกาท้ายไฟล์)
> **สาย** ([TRACKS.md](../TRACKS.md)): 🟦 FE → P0-02, P0-03 · 🟥 BE → P0-01, P0-04, P0-05, P0-06, P0-07, P0-08, P0-09

**เป้าหมาย:** รางรถไฟพร้อมวิ่ง — เว็บเปล่า deploy ได้จริง CI ตรวจครบ ยังไม่มีฟีเจอร์

---

### P0-01 — scaffold Next.js
- [ ] `npx create-next-app@latest` — App Router + TS + ESLint + Tailwind + `src/` + import alias `@/*`
- [ ] ลบ boilerplate หน้าแรก / โลโก้ / css ที่ไม่ใช้
- [ ] `next.config.ts` — `images.remotePatterns` ค้างไว้รอ Supabase Storage domain
- [ ] `npm run dev` ขึ้นหน้าเปล่าได้
- [ ] commit `P0-01: scaffold next.js app`

### P0-02 — Tailwind + design tokens
- [ ] อ่านตัวแปร `:root` จาก `mock-up.html:9–23`
- [ ] ใส่ `tailwind.config.ts` → `theme.extend.colors` (`bg paper ink muted line brand brand-2 soft danger`), `borderRadius.DEFAULT`, `boxShadow.card`
- [ ] font stack `Noto Sans Thai` + system ผ่าน `next/font` ใน root layout
- [ ] `components/ui/` primitives เปล่า: `Button` `Field` `Badge` ใช้ token
- [ ] commit

### P0-03 — next-intl (locale ใน URL)
- [ ] `npm i next-intl`
- [ ] `src/i18n/routing.ts` — `locales: ['th','en']`, `defaultLocale: 'th'`, `localePrefix: 'always'`
- [ ] `middleware.ts` — next-intl middleware (auth guard เพิ่มทีหลัง P5)
- [ ] `src/app/[locale]/layout.tsx` — `NextIntlClientProvider` + `setRequestLocale`
- [ ] `messages/th.json` + `messages/en.json` = `{}`
- [ ] `/[locale]/page.tsx` placeholder อ่าน 1 message
- [ ] เปิด `/th` และ `/en` ได้, `/` redirect → `/th`
- [ ] commit

### P0-04 — Supabase project + clients
- [ ] สร้าง project region `ap-southeast-1` (สิงคโปร์)
- [ ] `npm i @supabase/supabase-js @supabase/ssr` · `npx supabase init`
- [ ] `src/lib/supabase/client.ts` (browser, anon)
- [ ] `src/lib/supabase/server.ts` (server component, cookies, anon)
- [ ] `src/lib/supabase/admin.ts` (service role) — ขึ้นต้นด้วย `import 'server-only'`
- [ ] `npx supabase link` + `supabase db push` เปล่าผ่าน
- [ ] commit

### P0-05 — env + gitignore + README
- [ ] `.env.example` ครบทุกคีย์ตาม `PLAN.md` §11
- [ ] `.env.local` จริง (ไม่ commit) — Supabase URL/anon/service_role
- [ ] `.gitignore` — `.env*.local` `.next` `node_modules` `.vercel`
- [ ] `README.md` — clone → ตั้ง env → `npm run dev`
- [ ] `git status` ยืนยันไม่มี `.env.local` หลุด
- [ ] commit

### P0-06 — lint / format / typecheck
- [ ] `npm i -D prettier prettier-plugin-tailwindcss vitest`
- [ ] `.prettierrc` + eslint: `react/no-danger: error` (กัน `dangerouslySetInnerHTML`)
- [ ] scripts: `lint` `typecheck` (`tsc --noEmit`) `format` `test`
- [ ] รันทั้ง 4 เขียว
- [ ] commit

### P0-07 — CI (GitHub Actions)
- [ ] `.github/workflows/ci.yml` — trigger: PR + push `main`
- [ ] steps: `npm ci` → `typecheck` → `lint` → `build` (+ `test` เมื่อมี)
- [ ] ใส่ `NEXT_PUBLIC_*` placeholder ให้ `build` ผ่าน
- [ ] เปิด PR ทดสอบ → CI เขียวครบ
- [ ] commit

### P0-08 — deploy Vercel
- [ ] เชื่อม repo ↔ Vercel project
- [ ] ใส่ env vars (production + preview) ใน dashboard
- [ ] deploy `main` → เปิด URL เห็น `/th` `/en` placeholder
- [ ] ตั้ง `NEXT_PUBLIC_SITE_URL` = URL จริง
- [ ] _(D-2 Vercel Pro ยังค้าง — preview ใช้ไปก่อนได้)_

### P0-09 — Sentry
- [ ] `npx @sentry/wizard@latest -i nextjs`
- [ ] ตรวจ `sentry.*.config.ts` + `SENTRY_DSN` ใน env (local + Vercel + CI)
- [ ] หน้า test throw error → เห็น event ใน Sentry
- [ ] ลบหน้า test
- [ ] commit

---

## เกณฑ์ว่าเฟส P0 เสร็จ

- เปิด URL production เห็น placeholder ที่ `/th` และ `/en`
- PR เปล่าผ่าน CI ครบ (typecheck + lint + build)
- ยิง error แล้วเข้า Sentry

## กติกาปิด task

1. ติ๊ก subtask `- [x]` ระหว่างทำ · ครบแล้วติ๊กหัวข้อ `### P0-XX`
2. `../PROGRESS.md`: อัปเดตคืบหน้า `x / 9` + สถานะเฟส + บรรทัด "บันทึกความคืบหน้า"
3. commit `P0-XX: <สิ่งที่ทำ>`
4. เจอเรื่องต้องตัดสินใจ → เพิ่มแถวตาราง "เรื่องที่ติดอยู่" ใน `../PROGRESS.md` ทันที
